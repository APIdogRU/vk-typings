export interface IVideoTimelineThumbs {
    count_per_image: number;
    count_per_row: number;
    count_total: number;
    frame_height: number;
    frame_width: number;
    links: string[];
    is_uv: boolean;
    frequency: number;
}
